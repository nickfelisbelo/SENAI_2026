generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Carros {
  placa String @id
  marcaemodelo String
  ano Int
}

model Clientes {
  id Int @id @default(autoincrement())
  nome String
  matricula Int
  turmasId Int
} 